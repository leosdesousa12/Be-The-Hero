const express = require('express');

//creat application
const app = express();

app.get('/', (request, response) => {
    return response.json({
        pessoa: 'leonardo',
        idade:'24'
    });
});

//set port application
app.listen(3333);

